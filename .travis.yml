language: php

php:
  - 5.5
  - 5.6
  - 7.0
  - 5.5
    env: DEPS="low"
  - 5.6
    env: DEPS="low"
  - 7.0
    env: DEPS="low"
  - 7.1
  - hhvm

matrix:
  fast_finish: true
  allow_failures:
    - php: hhvm

cache:
  directories:
    - $HOME/.composer/cache

before_install:
  - phpenv config-rm xdebug.ini || true

install: if [[ "$DEPS" == "low" ]]; then composer update --prefer-dist --prefer-lowest --no-interaction --optimize-autoloader; else composer update --optimize-autoloader --prefer-dist --no-interaction; fi;

script: composer test
